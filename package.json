{
  "name": "loom-ai-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for Loom AI case intake — holds the Anthropic API key server-side so the browser never sees it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
